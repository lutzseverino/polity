name: Cardo Compatibility

on:
  schedule:
    - cron: "23 6 * * 1"
  workflow_dispatch:

permissions:
  contents: read

concurrency:
  group: cardo-compatibility-${{ github.ref }}
  cancel-in-progress: true

jobs:
  main:
    name: Cardo Compatibility / Main
    runs-on: ubuntu-24.04
    steps:
      - name: Check out Polity
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
      - name: Check out Cardo main
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          repository: lutzseverino/cardo
          path: cardo
          ref: main
      - name: Set up Java
        uses: actions/setup-java@b6effb05e454b25005698d916606bdc6ffcbf961 # v5.7.0
        with:
          distribution: temurin
          java-version: 21
          cache: maven
      - name: Install Cardo main
        run: ./cardo/mvnw --batch-mode --no-transfer-progress -f cardo/pom.xml install -DskipTests -Drevision=0.1.0-SNAPSHOT
      - name: Verify Polity compatibility
        run: ./mvnw --batch-mode --no-transfer-progress -f services/polity/pom.xml verify spotbugs:check -Dcardo.version=0.1.0-SNAPSHOT
